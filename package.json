{
  "name": "pogadaj-se",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "check": "for f in $(find js -name '*.js'); do node --check \"$f\" || exit 1; done && echo OK"
  }
}
